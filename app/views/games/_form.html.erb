<%= Match.find(:all, :include => [ :players ]) %>
<%= simple_form_for @game, :html => { :class => 'form-horizontal' } do |f| %>
	<%= f.association :match, :collection => Match.find(:all, :include => [ :matches_players ]) %>
	<%= f.association :map %>
  	s<%= f.button :submit, :class => 'btn-primary' %>
<% end %>
