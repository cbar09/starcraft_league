	<%= f.association :player, 
		:label => first ? 'Player A' : 'Player B', 
		:input_html => {:class => 'span2', :value => player.handle}, 
		:as => :string,
		:disabled => true  %>
	
	<%= f.association :race, 
		:label => first ? "Player A's Race" : "Player B's Race", 
		:as => :select, 
		:input_html => {:class => 'span2 race'}, 
		:include_blank => 'Select a race...' 
	%>
	
	<%= f.input :winner, 
		:label => false,
		:inline_label => 'Winner?', 
		:input_html => {:class => 'winner'}
	%>
	
	<script type="text/javascript">
	document.on()
	</script>