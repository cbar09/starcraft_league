<%= form_for(@matches_player) do |f| %>
  <% if @matches_player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@matches_player.errors.count, "error") %> prohibited this matches_player from being saved:</h2>

      <ul>
      <% @matches_player.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_id %><br />
    <%= f.number_field :player_id %>
  </div>
  <div class="field">
    <%= f.label :match_id %><br />
    <%= f.number_field :match_id %>
  </div>
  <div class="field">
    <%= f.label :winner %><br />
    <%= f.check_box :winner %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
